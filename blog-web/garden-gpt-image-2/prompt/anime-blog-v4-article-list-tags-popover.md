Edit the supplied article-list screenshot with one precise filter-layout correction while preserving the entire established anime railway design and article list.

Preserve exactly:
- The spring railway hero, "春日轨迹" wordmark, active "文章" navigation with small submenu chevron, large page title, subtitle, shallow white wave, sparse full-screen petals, typography, palette, spacing, five alternating article rows, railway separators, cover images, metadata and pagination.
- Preserve row alternation exactly: first text left / image right, second image left / text right, continuing through all five rows.
- Do not redesign the header or article content.

Required filter correction:
- Remove the permanent full-width tag bubble row currently showing label "标签" and bubbles such as "春日 / 铁路 / 摄影 / 治愈系 / 创作手记 / 雨天".
- The page must not reserve a separate horizontal row for tag bubbles.
- Create one compact main filter row only: keyword search field, "年份" selector, "月份" selector, and a quiet compact trigger labeled "更多标签" with a downward chevron on the far right.
- Keep the current-condition summary on the following lightweight line: "当前：全部文章 · 标签：铁路、摄影（任一匹配）" with small removable close icons and "清空条件" at the far right.

Show the tag selector popover OPEN in this static prototype:
- Anchor a lightweight white popover directly beneath the "更多标签" trigger, aligned to its right edge.
- Popover width around 300-340px, subtle 1px pale-gray border, maximum 8px corner radius, very soft shadow, compact padding. It must feel like a dropdown/popover, not a large card or modal.
- Popover heading "选择标签" with a small close X icon.
- Inside show equal-height compact selectable bubbles: "春日", "铁路", "摄影", "治愈系", "创作手记", "雨天".
- Exactly "铁路" and "摄影" are selected with soft sakura-pink fill and border; other bubbles are white with fine gray outlines.
- Add one small helper line "已选标签按任一匹配筛选". Do not add confirm, query, reset or apply buttons because selection updates immediately.
- The popover overlays available white space and does not push the article list downward or become a permanent section.

Readability:
- Keep petals faint around the filter and popover.
- Ensure the popover does not cover the first article title or important controls; position it within the whitespace between filters and results if necessary.
- Keep the interface literary and light, never admin-dashboard-like.

Strict exclusions:
- No permanent tag bubble row, no category row, no category chips, no tag cloud, no large modal, no extra sidebar, no backend terminology, no new features.
- Do not alter article row content or the full-screen sakura atmosphere.
- The result should look like the same screenshot with a precise popover interaction state captured.
