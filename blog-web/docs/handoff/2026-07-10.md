# 2026-07-10 会话交接

## 当前状态

- 当前分支：`articles-ui`。
- 当前 HEAD：`0ace958 feat: add admin article routes`。
- 工作区存在尚未提交的后台视觉重置改动，禁止 reset、checkout 或覆盖现有修改。
- `docs/superpowers/plans/2026-07-09-admin-articles-ui.md` 也处于修改状态，按用户现有内容继续使用，不要回滚或整体重写。

## 已完成

文章计划 Task 1-6 已完成，下个会话从 Task 7 开始：

- Task 1：文章 UI 设计文档实施确认，提交 `796da6c`。
- Task 2：BDD 行为空测试门禁，提交 `e2b3453`；门禁已经完成并获准继续，不要再次创建空测试或重复暂停询问。
- Task 3：后端删除响应契约修正，用户确认已完成。
- Task 4：编辑器解析、清理和浮层定位依赖，提交 `374d8d7`。
- Task 5：文章类型、API、Query 及测试，提交 `0461172`。
- Task 6：文章路由和占位页面，提交 `0ace958`。

后台登录、后台首页、管理员设置和后台框架已完成一轮未提交的视觉重置：

- 浅色冷中性工作台，使用 `Noto Sans SC Variable`。
- 侧边栏约 `236px`，顶部栏约 `60px`，顶部栏左侧显示当前页面名称。
- 内容区左对齐并随窗口扩展，最大宽度约 `1520px`，不再使用 `880px` 居中窄栏。
- 登录页使用 `44% / 56%` 左右分区；设置页使用完整宽度操作面板。
- 中性灰阶为主，低饱和赤陶红只用于当前项、链接和焦点；无渐变、无发光、无页面阴影。
- 按钮和输入框 `4px` 圆角，操作面板 `6px` 圆角；弹窗是唯一允许使用轻阴影的容器。
- 已新增 `AdminAvatar`，统一头像空值和加载失败回退。
- 未完成的文章管理入口当前继续隐藏，文章路由和源码仍保留。

视觉基线代码：

- `src/styles/variables.css`
- `src/components/admin/admin.css`
- `src/components/ui/ui.css`
- `src/views/admin/adminPages.css`
- `docs/UI设计目录/后台管理员设置/2026-07-06-后台登录与管理员设置设计.md`

真实浏览器证据：

- `output/playwright/admin-reset-dashboard-after-v2.png`
- `output/playwright/admin-reset-settings-after-v2.png`
- `output/playwright/admin-reset-logout-dialog-after.png`
- `output/playwright/admin-reset-login-validation-after.png`

## 下个会话目标

从 `docs/superpowers/plans/2026-07-09-admin-articles-ui.md` 的 Task 7 开始，完成 Task 7-20，不重复 Task 1-6。

1. Task 7-8：完成文章模块小组件、文章列表筛选、横向文章项、分页、空状态、错误状态和删除弹窗。
2. Task 9-17：完成块模型、历史、Markdown 解析与序列化、块画布、块菜单、文字工具、列表、表格、粘贴、图片块和快捷键抽屉。
3. Task 18：完成新建和编辑共用页面、右侧文章信息区、保存、删除及 `Ctrl+S`。
4. Task 19：按真实浏览器完成文章列表、编辑页、浮层和弹窗视觉验收并迭代。
5. Task 20：完成类型、测试、Lint、格式、构建和真实后端流程回归。

## 视觉实施要求

- 文章页面必须继承本次重置后的后台视觉语言，不再沿用旧蓝色功能主义主题、旧卡片堆叠或旧原型的装饰风格。
- 旧文章原型只可用于确认内容结构和交互关系，不能作为颜色、尺寸、圆角、阴影和页面宽度的视觉权威。
- 开始 Task 7 代码前，小范围更新现有 `docs/UI设计目录/文章UI/2026-07-08-后台文章模块UI设计.md`，加入当前后台视觉基线；禁止新建设计文档。
- 文章列表使用当前 `PageHeader`、按钮、输入框、弹窗、反馈和内容宽度；不要重新造一套后台组件。
- 文章项应便于扫描和重复操作，避免营销页式大卡片、彩色徽章、图标方框和无意义统计。
- 编辑页保持工作台密度：左侧连续文档画布，右侧信息区清晰稳定；普通块不能做成一块一个卡片。
- 编辑器浮层只在操作时出现，不能造成主布局跳动，也不能遮挡当前编辑位置。
- 文章列表和编辑页真正可用前继续隐藏侧边栏文章入口；Task 7-18 完成并通过真实浏览器检查后再恢复入口。

## 浏览器验收要求

- 使用用户正常桌面浏览器窗口，不向 Playwright 传入固定 viewport，也不要按计划中的 `1440x900`、`1536x960` 命令机械截图。
- 当前重置验收使用的真实可视区域为 `1920x945`，仅作为本机事实，不是新的硬编码尺寸。
- 后端由用户自行启动；前端启动必须遵守仓库规则，使用普通可见 `cmd` 窗口，不使用脚本、`Start-Process` 或自写启动器。
- 登录凭据不得写入代码、测试、文档、日志或交接文件；需要时直接向用户获取。
- 验收至少覆盖文章列表、筛选、空结果、加载失败、新建、编辑、保存、删除、封面失败、各编辑器浮层和退出登录。

## 已有验证

- `npm run typecheck`：通过。
- `npm run test:run`：20 个测试文件、97 项测试通过。
- `npx eslint src --max-warnings=0`：通过。
- 本次改动文件 Prettier 检查：通过。
- `npm run build`：通过。
- 真实浏览器已验证登录、后台首页、管理员设置、资料保存、Token 校验与刷新、头像失败回退、退出确认和登录必填错误。

全仓库 `npm run lint` 和 `npm run format:check` 当前会被未跟踪的 `.agents/` 技能文件影响；格式检查还会报告两个既有文件 `src/api/admin.test.ts`、`src/utils/request.test.ts`。不要为了本任务批量格式化或修改无关文件，先对文章模块和实际改动文件执行精确检查。

## 重要边界

- 不实现分类、标签、置顶、推荐、批量删除、文件上传、正文图片上传、Markdown 源码视图、自动保存、移动端或前台文章展示。
- 不引入完整后台 UI 库或完整富文本编辑器框架。
- 页面不得直接调用 Axios；继续使用现有 `types -> api -> queries -> views` 边界。
- 不修改与文章 UI 无关的用户改动，不处理 `.agents/`、`.playwright-cli/`、`canvas/`、`garden-gpt-image-2/` 等未跟踪内容。
- 当前后台视觉重置尚未提交。实施文章 UI 时必须在现有工作区上继续，不能退回旧后台样式。

## 追加交接：Task 7-12 已完成

本段为当前最新状态，覆盖本文前面“下个会话从 Task 7 开始”和“文章入口继续隐藏”等旧说明。

### 当前状态

- 当前分支：`articles-ui`。
- 当前 HEAD：`7512e20 后台重置`。
- Task 7-12 的实现、测试和文章侧边栏入口均在工作区中，尚未提交；禁止 reset、checkout、restore 或覆盖这些改动。
- BDD 门禁此前已经完成并获准继续；下个会话不要重复创建空测试，也不要再次为 Task 13-14 暂停询问。

### 已完成事项

- Task 7：文章封面、状态标签、保存状态和文章页面样式基座。
- Task 8：真实文章列表、关键词与状态筛选、横向文章项、分页、加载/错误/空状态和删除确认弹窗。
- Task 9：编辑器块模型、块命令、表格行列命令和撤销重做历史。
- Task 10：Markdown 解析与序列化、GFM 表格、受限 HTML 表格和危险 HTML 清理。
- Task 11：连续文档画布及段落、标题、引用、列表、代码、图片、表格、分割线基础渲染。
- Task 12：块旁新增菜单、`/` 菜单键盘操作和 Markdown 快捷转换。
- 侧边栏已开放“文章管理”，点击跳转 `/admin/articles`，文章子路由下保持 active；顶部栏文章路由显示“文章管理”。
- 文章 UI 设计文档已补充当前后台视觉基线、本轮 Task 7-12 边界和文章入口开放条件。

### 验证结果

- 全量 `npm run test:run`：20 个测试文件、102 项测试通过。
- `npm run build`：通过。
- 本轮精确 ESLint 和代码 Prettier 检查：通过。
- 追加侧边栏后定向测试：8 个测试文件、39 项测试通过。
- 追加侧边栏后 `npm run typecheck`：通过。
- 真实后端联调已验证登录、文章列表、关键词与状态组合筛选、删除确认弹窗和取消操作；页面控制台无 error。
- 登录凭据没有写入代码、测试、文档或交接文件；需要再次联调时向用户获取。

### 下个会话目标

严格按 `docs/superpowers/plans/2026-07-09-admin-articles-ui.md` 顺序执行：

1. Task 13：实现块工具浮层，完成定位、类型切换、插入、移动、复制、删除、禁用规则和测试。
2. Task 14：在 Task 13 完成并验证后实现文字工具浮层，完成选区检测、格式命令、链接、预设色板、清除格式和测试。
3. Task 14 验证完成后停止，不进入 Task 15 列表交互，也不提前实现表格工具、粘贴、快捷键抽屉或文章编辑页。

### Task 13-14 重要约束

- Task 13 使用现有 `@floating-ui/react-dom`，reference 为当前块操作柄，使用 `offset`、`flip`、`shift` 和 `autoUpdate`。
- Task 14 的文字颜色和背景高亮只能使用 `markdownSchema.ts` 的预设色板，格式结果必须经过现有 HTML 清理边界。
- 工具浮层必须是 overlay，不得改变块尺寸、挤压连续文档画布或遮挡当前编辑位置。
- `readOnly` 不显示工具浮层；Esc 只关闭当前浮层。
- 继续复用现有块命令、Markdown 转换和编辑器状态，不重写 Task 9-12 已完成的核心。
- 不恢复旧蓝色主题，不把普通块做成卡片，不修改与文章编辑器工具无关的用户改动。
